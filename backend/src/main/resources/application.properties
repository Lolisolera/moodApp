pexels.api.key=${PEXELS_API_KEY:}
server.port=8080
spring.application.name=moodapp

# Load optional local secrets (kept out of VCS)
spring.config.import=optional:classpath:application-secrets.properties
